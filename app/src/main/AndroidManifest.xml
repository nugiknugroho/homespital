<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nugik.myapplication">
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/launcherbiru"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Splash"
            android:theme="@style/SplashPage"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LogRegister.MainActivity"
            android:theme="@style/LoginPage"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".LogRegister.Main2Activity"
            android:theme="@style/RegisterPage"
            android:windowSoftInputMode="adjustResize" />

        <activity android:name=".FragmentMenuLayanan.HomeActivity"
            android:theme="@style/DokterPage"/>
        <activity
            android:name=".DetailActivityMenu.DokterActivity"
            android:theme="@style/DokterPage"
            >

        </activity>
        <activity
            android:name=".DetailActivityMenu.BidanActivity"
            android:theme="@style/DokterPage">

        </activity>
        <activity
            android:name=".DetailActivityMenu.PerawatActivity"
            android:theme="@style/DokterPage">

        </activity>
    </application>

</manifest>